apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: stud-reg-cluster
  region: ap-south-1
  version: "1.30"

managedNodeGroups:
  - name: standard-nodes
    instanceType: t3.medium
    desiredCapacity: 2
    minSize: 2
    maxSize: 4
    volumeSize: 20
    ssh:
      allow: true
      publicKeyName: docker11  # Ensure your public key is in AWS
